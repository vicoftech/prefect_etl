deployments:
  - name: etl_filters_deployment
    entrypoint: etl_prefect.py:etl_flow
    work_pool:
      name: "default-agent-pool"
    parameters:
      input_file: "input.csv"  # Cambia por tu pool en Prefect Cloud